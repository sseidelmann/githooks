#!/usr/bin/php
<?php

define('VENDOR_DIRECTORY', realpath(dirname(__FILE__) . '/../../../') . DIRECTORY_SEPARATOR);

include VENDOR_DIRECTORY . 'autoload.php';

$loader = new \GitHooks\HookLoader($argv);
$loader->run();